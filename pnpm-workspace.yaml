packages:
  - '.'
  - 'docs'

onlyBuiltDependencies:
  - "@biomejs/biome"
  - es5-ext
  - esbuild
  - sharp

# Known, unfixable dev-only advisories. None affect the published package —
# raqam ships zero runtime dependencies, so nothing here reaches a consumer.
# Revisit when the parent releases a patched version.
#   esbuild <- storybook > @storybook/builder-vite (vite 8 pins esbuild 0.27)
auditConfig:
  ignoreGhsas:
    - GHSA-g7r4-m6w7-qqqr # esbuild: file read via dev server, Windows only
